$(document).ready(function(){
    // نضيف التنسيقات من داخل الكود

    // عند الضغط على زر التالي
    $("#nextBtn").click(function(){
        $("#no-products-message").fadeIn(300).delay(3000).fadeOut(300);
    });
});
